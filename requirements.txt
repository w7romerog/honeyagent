# HoneyAgent — dependencias Python
# Instalar con: pip install -r requirements.txt

# AWS SDK
boto3>=1.34.0

# Claude API (Anthropic)
anthropic>=0.40.0

# HTTP client (Slack webhook, AbuseIPDB)
requests>=2.32.0

# Generación de reportes PDF
reportlab>=4.2.0

# Parsing de YAML (honeypots.yaml)
pyyaml>=6.0.1

# Variables de entorno locales (.env)
python-dotenv>=1.0.0
