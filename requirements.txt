# HoneyAgent — dependencias Python
# Instalar con: pip install -r requirements.txt

# AWS SDK
boto3>=1.34.0

# Claude vía Amazon Bedrock (AnthropicBedrock) — no se usa la API directa de
# Anthropic; la autenticación es con credenciales/rol de IAM, no API key.
anthropic>=0.40.0

# HTTP client (ip-api.com para lookup_ip_reputation)
requests>=2.32.0

# Parsing de YAML (honeypots.yaml)
pyyaml>=6.0.1

# Variables de entorno locales (.env)
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-env>=1.1.0
